fn main() {
    // 任务 1：定义 score = 92。

    // 任务 2：使用 if / else if / else 得到 grade，
    // 分数 >= 90 为 "A"，>= 80 为 "B"，其他为 "C"。

    // 任务 3：使用 if 表达式得到 status，
    // score >= 60 时为 "pass"，否则为 "fail"。

    // 任务 4：使用 loop，让计数器从 0 开始累加，
    // 当计数器等于 3 时，使用 break 返回 6。

    // 任务 5：使用 while 打印倒计时：
    // 3
    // 2
    // 1

    // 任务 6：使用 for 遍历 [10, 20, 30]，
    // 依次打印每个值。

    // 目标输出：
    // grade: A
    // status: pass
    // loop result: 6
    // countdown:
    // 3
    // 2
    // 1
    // values:
    // 10
    // 20
    // 30
}
