fn main() {
    // 任务 1：先用 `cargo run` 直接运行这个二进制。
    // 任务 2：再用 `cargo install --path .` 把它安装到本机。
    // 任务 3：安装后直接在终端运行 `hello-install`。
    println!("hello from cargo install");
    println!("binary name: hello-install");
}
