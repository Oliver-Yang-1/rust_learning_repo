fn main() {
    // 任务 1：先用 `cargo run` 直接运行这个二进制。
    // 任务 2：用 `cargo install --path .` 安装它。
    // 任务 3：安装后执行 `cargo hello-study`，观察它像 Cargo 子命令一样工作。
    println!("custom cargo command works");
    println!("subcommand name: hello-study");
}
