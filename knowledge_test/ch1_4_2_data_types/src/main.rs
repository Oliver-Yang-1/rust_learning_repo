fn main() {
    // 任务 1：把 "42" 解析为 u32，变量名用 guess。
    // 提示：这一步建议显式写出类型。

    // 任务 2：定义一个 f64 浮点数，值为 36.5。

    // 任务 3：定义一个 bool，值为 true。

    // 任务 4：定义一个 char，值为 'R'。

    // 任务 5：创建元组 (500, 6.4, 1)，并取出第二个值。

    // 任务 6：创建数组 [10, 20, 30, 40, 50]，
    // 打印第一个元素和最后一个元素。

    // 目标输出：
    // guess: 42
    // temperature: 36.5
    // is_rust_fun: true
    // initial: R
    // tuple second: 6.4
    // array first: 10
    // array last: 50
}
